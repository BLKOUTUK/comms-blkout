# BRIEF — Admin cleanup + Funding "Income" tab
**10 September 2026.** Rob: *"there remain dashboard modules that still contain mock data and/or serve no useful purpose"* → audit verdict accepted → *"On funding, a tab that includes subscriptions and earned income"* → *"go"*. One Opus agent, one comms PR, plus one edit in the books repo.

## Facts (verified 10 Sep, live)
- **Agents**: nothing has produced a `socialsync_agent_tasks` row since **29 Jan 2026** (crawler triggers dropped today). Pending non-crawler: weaver 9 · griot 2 · herald 1 · brand_guardian 1 · viral_trends 1 · listener 1 · event_scheduler 1 (one griot task was approved into the register today). `agent_configurations`: 6 rows, all active, last updated Dec 2025; **nothing outside the admin reads `is_active`** (grep of ivor-core, comms api, news-blkout: none). `agent_intelligence_feed` view (5 rows) sits over `ivor_intelligence` (3,975 rows, refreshed 4×/day by ivor-core) — only the herald generator uses it server-side. `useAgentIntelligence.ts` carries a `📦 Mock:` branch.
- **Settings**: six toggles (write nothing reads) + a "where things live" card.
- **Newsletters** (`Newsletters.tsx` 1,129 lines; `components/newsletters/TemplateLibrary.tsx` 729): live = `newsletter_editions` 12 rows · `newsletter_content_items` 42 (last write today); dead = `newsletter_subscribers` **0 rows** (subscriber-tiers panel), the template library, the intelligence panel (`useAgentIntelligence('herald')`).
- **Funding**: Pipeline + Opportunities read live `grant_pipeline` (views `opportunity_pipeline` 10, `bid_writing_progress` 8); Bid Writing tab = `bid_writing_templates` 5 rows (Dec 2025); CF Outreach tab = `cf_fundraising_drafts` 17 rows, all draft, last touched 21 May.
- **Books** (`~/blkout/projects/financial-management/`, now a git repo, author Rob, auto-commit+push in `build-finance.mjs`): `chart-of-accounts.md` income codes — restricted 4001–4004 (one per grant) · **4100 membership subscriptions** (Zeffy) · **4200 trading — shop and pre-orders** · **4300 events and ticketing** · **4400 donations (incl. director-borne costs)** · **4500 fees for services and commissions**; balance code **1100 Zeffy payouts receivable**. `ledger.json` = the coded transactions. `build-finance.mjs` publishes nine `finance.*` measures to `metrics.snapshots`; `public.admin_finance_snapshot()` returns the latest row per `finance.*` measure generically, so **a new measure flows to `/api/admin/finance` with no migration**. `metrics.memberships_by_tier` reaches the page via `/api/admin/dashboard`.
- Financial year: first period 24 Nov 2025 – 30 Sep 2026; FY2 from 1 Oct 2026. ⚠ The chart says subscriptions "from 1 Oct 2026"; the agreed calendar opens membership on **Beam Day, 28 Dec 2026** — the tab says 28 Dec; **do not edit the chart** (Rob's call), flag it in the report.

## A. Retire Agents (page, hooks, cards) — keep the two valuable parts
1. **Move the January drafts into the register.** For every `socialsync_agent_tasks` row with `status='pending'`, `agent_type <> 'news_crawler'` and non-empty `generated_content`: insert a `content_calendar` row exactly as today's approval path does (`admin_content_insert`, status `draft`, `generated_by_agent` = agent_type, `primary_content` from generated_content, `metadata.source = 'agent:<task id>'`, `metadata.channels` from target_platform, `internal_notes` = "moved from the agents queue 10 Sep 2026 (task created <date>)"); then set the task `status='cancelled', approval_notes='moved to the content register 10 Sep 2026 → <row id>'`. Tasks with empty content → `cancelled`, note "no generated content; agents queue retired 10 Sep 2026". Do it with the writer MCP in one transaction; print before/after counts and the new register ids.
2. **`ValuesCheck`** → `src/components/content/ValuesCheck.tsx`, wired as the confirmation on the Content page's **draft → ready** transition (the checklist must be ticked before the status call fires; ready → draft/skipped need no check). Keep its wording.
3. Delete: `src/pages/admin/Agents.tsx`, `src/components/agents/*`, `src/components/shared/AgentCard.tsx`, hooks `useAgents`, `useAgentTasks`, `useAgentActivity`, `useAgentIntelligence`; the `/admin/agents` route (add a redirect to `/admin/calendar` so old links land somewhere); the sidebar entry. Dashboard: remove the AI Agents cards, the Recent Activity panel and the "Agent content awaiting approval" tile (four tiles remain: events 7d, moderation queue, live bids, members); quick action "Review agent content" → "Content register" → `/admin/calendar`.

## B. Retire Settings
Move the "where things live" card into `Account.tsx` (below the password form; update its text to today's homes: content register, books, Mission Control upload, newsletter skill, image skill, routines). Delete `Settings.tsx`, its route (redirect → `/admin/account`) and sidebar entry.

## C. Newsletters — cut to the working core
Keep: editions list, generate, preview, export, Send to SendFox, and the editorial prompt/reply **only if** the server side is live — check the comms Coolify runtime vars by NAME (`RESEND_API_KEY`, `EDITOR_EMAIL`) via `mcp__coolify__env_vars` list (masked); if either is absent, remove the editorial UI and say so. Remove: the subscriber-tiers panel and every `newsletter_subscribers` read in `useNewsletter.ts`; `TemplateLibrary` / `TemplateQuickSelect` and the file `TemplateLibrary.tsx`; the intelligence panel (`useAgentIntelligence`). Target well under 700 lines; every remaining control must do something that persists or sends.

## D. Funding — drop the two stale tabs, add Income
Remove the CF Outreach tab (`useFundraisingDrafts`, its sections) and the Bid Writing tab (templates list); keep Pipeline and Opportunities (and the bid-progress view if it is rendered inside those). Add **Income** (third tab):
- **Grants** — per fund from the books (`finance.funds.detail.funds`: awarded · received · receivable · spent · remaining) + the pipeline stage counts already on the page.
- **Subscriptions** — `memberships_by_tier` (sum + per tier; when zero: "opens 28 Dec 2026"), the three tiers as text (free · £3/mo · £10/mo, via Zeffy), and **Zeffy payouts receivable** (code 1100) from the new measure below — "not recorded" when null.
- **Earned** — totals to date for 4200 trading, 4300 events, 4500 fees (and the sum), with **Donations (4400) shown separately and labelled "not earned income"**; as-at date from the measure's detail.
States: loading "…" · failed red panel · a real zero renders as £0.00 with its as-at date · a missing measure renders "The books have not published income by code yet — run build-finance". No `?? 0` on fetched values.

## E. The new measure — `~/blkout/projects/financial-management/build-finance.mjs`
Add `finance.income_by_code` to the publish list: `value` = total income to date across all 4xxx codes (numeric, £); `detail` = `{ as_at, fy_start, fy_end, codes: [{code, name, restricted, total_to_date, this_fy}], zeffy_receivable: <code 1100 balance or null>, unrestricted_earned: <4200+4300+4500 to date>, donations: <4400 to date>, subscriptions: <4100 to date> }`, computed from `ledger.json` rows by account code using the script's own sign convention (read how `cash`/`funds` are computed; income is credit-side). Read `chart-of-accounts.md` for the code names. Keep `--no-publish`/`--no-commit` semantics. Then run `node build-finance.mjs` for real: it publishes (10 measures now), auto-commits your edit with an `(RB)` message and pushes to the VPS mirror — show the commit hash locally and on `root@72.61.201.5:/opt/blkout-books.git`. Never print the access token.

## Hard rules
Worktree `~/wt/comms-admin-cleanup` (branch `claude/admin-cleanup-income` from `origin/main`); `npm ci --legacy-peer-deps` if plain `npm ci` fails; no lockfile changes; never touch `remotion-videos/`. No secrets in client code. No fallback that parses as a result. No Canva. Copy this brief into the worktree `docs/` and include it in the PR. Do not push to main. Do not spawn sub-agents. Do not edit `chart-of-accounts.md` or any migration. Order: E → A → B → C → D → proofs → commit → PR.

## Proof (verbatim)
- A1 counts: pending non-crawler before; register rows created (ids); tasks cancelled after; `select count(*) from socialsync_agent_tasks where status='pending'` → 0.
- `npm run build` clean; `grep -c "agent_configurations\|newsletter_subscribers\|TemplateLibrary\|cf_fundraising\|bid_writing_templates\|Mock:" dist/assets/*.js` → 0; bundle size before/after.
- Local server: `/api/admin/dashboard`, `/finance`, `/content` → 401 unauthenticated (unchanged); `/admin/agents` and `/admin/settings` → the SPA shell (redirects are client-side) — show the route table in `App.tsx`.
- Direct finance handler run past the guard: `books['finance.income_by_code']` present with `detail.codes` listing the 4xxx codes and `zeffy_receivable`.
- Books: `git log --oneline -2` locally and on the VPS after the real run; the `Published to metrics.snapshots (period …, 10 measures)` line.
- Sidebar entries after: Dashboard · Events · News · Content · Funding · Finance · Newsletters · Account.
- `git diff --stat`; PR URL.

## Report shape
Done / not done per section; proof verbatim; what the brief got wrong; orphan tables left (agent_configurations, socialsync_agent_tasks, newsletter_subscribers, bid_writing_templates, cf_fundraising_drafts — data untouched, listed for a later drop); PR URL.
